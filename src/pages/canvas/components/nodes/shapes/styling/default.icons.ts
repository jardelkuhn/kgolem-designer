import styled from "styled-components";

import { iconStyle } from "./default.theming";

export const FamilyIcon = styled.i`
  z-index: 2;
  font-size: 10px;
  font-color: ${iconStyle.color};
`;

export const ProviderIcon = styled.i`
  font-size: 8px;
  font-color: ${iconStyle.color};
`;
