import styled from "styled-components";
import { iconFont } from "./fonts";

export const FamilyIcon = styled.i`
  z-index: 2;
  font-size: 10px;
  font-color: ${iconFont.color};
`;

export const ProviderIcon = styled.i`
  font-size: 8px;
  font-color: ${iconFont.color};
`;
